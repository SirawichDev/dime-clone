{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "nuxt": "3.0.0-rc.10",
    "nuxt-windicss": "^2.5.2"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  },
  "lint-staged": {
    "components/**/*.{vue}": "prettier --write",
    "layouts/**/*.{vue}": "prettier --write"
  }
}
